** <%= notification.title %>
-------------------------------------------------------------------------------
<%= notification.notifiable.content %>

(<%= notification.created_at.strftime("%a, %d %b %Y at %l:%M%P %Z") %>)


Open Notification: <%= notification_url(notification) %>
