** <%= revision_notification.title %>
-------------------------------------------------------------------------------
<%= revision_notification.notifiable.title %>

(<%= revision_notification.created_at.strftime("%a, %d %b %Y at %l:%M%P %Z") %>)


Open Notification: <%= notification_url(revision_notification) %>
