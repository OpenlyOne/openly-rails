- if @ancestors.present?
  = render partial: 'ancestors',
           object: @ancestors,
           locals: { current_folder: @folder_diff.file_is_or_was,
                     project: @project }

.spacing.v48px

.container
  .row
    - @file_diffs.each_slice(2) do |slice|

      - slice.each do |file_diff|

        .col.s6.m3.l2.center-align
          = render partial: 'file_diff',
                   object: file_diff,
                   locals: { project: @project }

      div class=cycle('hide-on-med-and-up',
                      'hide-on-large-only',
                      'hide-on-med-only',
                      'hide-on-large-only',
                      'hide-on-med-and-up',
                      '')
        .clearfix
        .spacing.v48px

/ button for committing changes
- if @user_can_commit_changes
  .fixed-action-btn
    = link_to 'Commit Changes',
        new_profile_project_revision_path(@project.owner, @project),
        class: 'btn btn-large primary-color primary-color-text'
