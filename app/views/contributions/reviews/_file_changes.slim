/ Renders the changes of a file (file diffs)

/ By default, do not wrap each file change inside a layout
- file_change_wrapper ||= false

.revision-diff
  - if file_changes.any?
    = render partial: 'file_change',
             layout: file_change_wrapper,
             collection: file_changes,
             locals: { project: project,
                       revision: revision,
                       contribution: contribution,
                       path_parameters: path_parameters,
                       file_change_path: file_change_path,
                       show_link_to_file_info: show_link_to_file_info,
                       all_links_in_new_tab: all_links_in_new_tab }
  - else
    i No changes suggested.
